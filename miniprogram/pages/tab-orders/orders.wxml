<!-- 订单栏 -->
<view wx:key="this" wx:for="{{user_orders}}">
  <!--用wx:for绑定good数组，用数组中各项的数据重复渲染该组件-->
  <!--此个列表是动态列表，要加wx:key字段-->

  <van-card 
  desc="订单创建时间:{{ item.submission_time }}  订单预计送达：{{item.dilivery_time}}" 
  title="订单编号：{{ item._id }}"
  >
  </van-card>

</view>